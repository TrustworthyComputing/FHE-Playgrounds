int main(void) {
  EncInt[] word, search;
  EncInt counter;
  // Define EncInt and ints here as needed.
  // EncInt x;
  // int y;

  word = {0, 19, 0}; // "ata"
  search = {0, 0, 6, 2, 6, 0, 6, 2, 6, 0, 19, 0, 19, 0, 19, 0}; // "aagcgagcgatatatat"

  // Server side encrypted computation. //
  counter = 0;
  tstart();

  // TODO: Perform string similarity similarly to the python file.

  tstop();

  // Decrypt
  print(counter);
  return 0;
}
